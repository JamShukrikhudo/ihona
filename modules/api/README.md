# API module index

Each API package is a thin, versioned transport adapter over exactly one core
package. It enforces authentication, team context, validation, authorization,
pagination, bounded input, and documented response contracts.

| Project | Index |
| --- | --- |
| Real Estate | projects/real-estate/api/README.md |
| Liberu platform | projects/liberu/api/README.md |
