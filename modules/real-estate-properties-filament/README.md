# Real Estate Properties Filament

Filament 5 presentation adapter for the Properties module. The plugin is
explicitly attached by the host panel and intentionally contains no domain
models, persistence, or authorization decisions. The resource surfaces
listing metadata, region/city/district location, energy/accessibility scores,
virtual tours, and featured flags while lifecycle writes remain delegated to
the core actions.
