# Real Estate Viewings Livewire

The adapter includes list and booking components with availability slots,
stale-slot protection, and accessible loading, empty, success, and error
states.

Livewire 4 viewing list. Composer is unprefixed; GitHub source uses the `module-` prefix.
