# Liberu core modules

The current application consumes the existing foundation core packages:
application, identity, organizations and teams, settings, audit, search,
notifications, files and media, observability, scheduler and queues, and
supporting contracts.
